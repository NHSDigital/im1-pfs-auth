{
  "name": "im1-pfs-auth-api",
  "version": "0.0.1",
  "description": "OAS (Swagger v3) API Definition for IM1 PFS Auth API",
  "scripts": {
    "lint": "node_modules/.bin/openapi-generator-cli validate -i specification/im1-pfs-auth-api.yaml",
    "format": "./node_modules/.bin/prettier --write .",
    "spec-compile": "./node_modules/.bin/openapi-generator-cli generate -i specification/im1-pfs-auth-api.yaml --generator-key v3.0  -g openapi -o build && rm openapitools.json && mv build/openapi.json build/im1-pfs-auth-api.json"
  },
  "author": "NHS Digital",
  "license": "MIT",
  "homepage": "https://github.com/NHSDigital/im1-pfs-auth-api",
  "devDependencies": {
    "@apideck/portman": "~=1.32.1",
    "@openapitools/openapi-generator-cli": "~=2.20.2",
    "apigeetool": "~=0.16.4",
    "license-checker": "~=25.0.1",
    "minimist": "~=1.2.2",
    "newman": "~=6.2.1",
    "prettier": "~=3.6.2"
  }
}
