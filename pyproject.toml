[project]
name = "im1-pfs-auth"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask",
]

[project.optional-dependencies]
dev = [
  "pytest",
  "ruff"
]
